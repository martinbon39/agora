{
  "name": "agora",
  "private": true,
  "version": "0.1.0",
  "description": "Agora \u2014 self-hosted cockpit for Claude Code and CLI agent sessions",
  "license": "MIT",
  "author": "Martin Bonan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/martinbon39/agora.git"
  },
  "bugs": {
    "url": "https://github.com/martinbon39/agora/issues"
  },
  "homepage": "https://github.com/martinbon39/agora#readme",
  "keywords": [
    "claude-code",
    "agents",
    "terminal",
    "tmux",
    "self-hosted",
    "xterm",
    "canvas"
  ],
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server",
    "test": "npm run build -w server && node deploy/gate-canvas.mjs && node deploy/gate-chat.mjs && node deploy/gate-scope.mjs && node deploy/gate-accounts.mjs && node deploy/gate-peek.mjs && node deploy/gate-paths.mjs && node deploy/gate-tenants.mjs && node deploy/gate-credentials.mjs && node deploy/gate-hooktokens.mjs && node deploy/gate-plan.mjs"
  }
}
